<!DOCTYPE html>
<html>
  <head>
    <title>Build Performance Report</title>
    <style>
      body { font-family: Arial, sans-serif; margin: 20px; }
      
      .performance-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
        gap: 20px;
        margin-bottom: 30px;
      }
      
      .performance-card {
        border-radius: 8px;
        overflow: hidden;
        box-shadow: 0 3px 10px rgba(0,0,0,0.1);
      }
      
      .card-header {
        padding: 15px;
        background-color: #2196F3;
        color: white;
        text-align: center;
        font-weight: bold;
      }
      
      .card-body {
        padding: 20px;
        text-align: center;
      }
      
      .value {
        font-size: 32px;
        font-weight: bold;
        margin: 10px 0;
      }
      
      .unit {
        color: #666;
        font-size: 14px;
      }
      
      .chart-container {
        padding: 20px;
        background-color: #f5f5f5;
        border-radius: 8px;
        margin-bottom: 30px;
      }
      
      table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
      }
      
      th, td {
        border: 1px solid #ddd;
        padding: 12px;
        text-align: left;
      }
      
      th {
        background-color: #f2f2f2;
      }
      
      pre {
        background-color: #f5f5f5;
        padding: 15px;
        border-radius: 5px;
        overflow-x: auto;
      }
    </style>
  </head>
  <body>
    <h1>Build Performance Report</h1>
    
    <div class="performance-grid">
      <div class="performance-card">
        <div class="card-header">Build Time</div>
        <div class="card-body">
          <div class="value"><%= (buildTime / 1000).toFixed(2) %></div>
          <div class="unit">seconds</div>
        </div>
      </div>
      
      <div class="performance-card">
        <div class="card-header">Module Count</div>
        <div class="card-body">
          <div class="value"><%= moduleCount %></div>
          <div class="unit">files</div>
        </div>
      </div>
      
      <div class="performance-card">
        <div class="card-header">Average Complexity</div>
        <div class="card-body">
          <div class="value"><%= averageComplexity.toFixed(1) %></div>
          <div class="unit">per file</div>
        </div>
      </div>
      
      <div class="performance-card">
        <div class="card-header">Total Complexity</div>
        <div class="card-body">
          <div class="value"><%= totalComplexity %></div>
          <div class="unit">points</div>
        </div>
      </div>
    </div>
    
    <div class="chart-container">
      <h2>Build Time Breakdown</h2>
      <div id="buildTimeChart" style="height: 300px;">
        <!-- Placeholder for chart -->
        <p>Chart visualization would be rendered here using build stats data</p>
      </div>
    </div>
    
    <h2>Size Analysis</h2>
    <div class="performance-grid">
      <div class="performance-card">
        <div class="card-header">Input Size</div>
        <div class="card-body">
          <div class="value"><%= (buildStats.inputSize / 1024).toFixed(1) %></div>
          <div class="unit">KB</div>
        </div>
      </div>
      
      <div class="performance-card">
        <div class="card-header">Output Size</div>
        <div class="card-body">
          <div class="value"><%= (buildStats.outputSize / 1024).toFixed(1) %></div>
          <div class="unit">KB</div>
        </div>
      </div>
      
      <div class="performance-card">
        <div class="card-header">Compression Ratio</div>
        <div class="card-body">
          <div class="value"><%= (buildStats.compressionRatio * 100).toFixed(0) %>%</div>
          <div class="unit">of original</div>
        </div>
      </div>
      
      <div class="performance-card">
        <div class="card-header">Files Generated</div>
        <div class="card-body">
          <div class="value"><%= buildStats.files %></div>
          <div class="unit">files</div>
        </div>
      </div>
    </div>
    
    <h2>Detailed Build Statistics</h2>
    <pre><%= JSON.stringify(buildStats, null, 2) %></pre>
  </body>
</html> 